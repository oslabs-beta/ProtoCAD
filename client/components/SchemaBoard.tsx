import * as React from 'react';
//import CodeMirror from 'codemirror';

//let myCodeMirror = CodeMirror(document.body);



export default props => {
   return <div id={'schemaBoard'}>Schema Board Content</div>
};